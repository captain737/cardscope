requests>=2.31.0
beautifulsoup4>=4.12.0
PyYAML>=6.0
python-dotenv>=1.0.0

# LLM providers -- install whichever you're using (or both).
# Default is Gemini (LLM_PROVIDER=gemini in .env).
google-genai>=0.3.0
groq>=0.11.0

# Database -- only used when SUPABASE_URL/SUPABASE_SERVICE_KEY are set.
supabase>=2.9.0
